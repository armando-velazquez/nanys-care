import 'package:flutter/material.dart';

/// Paleta de colores de Nanys Care.
/// Los valores fueron extraídos de los mockups oficiales del proyecto.
class AppColors {
  AppColors._();

  // Morados (rol Tutor / acciones principales)
  static const Color primary = Color(0xFF6A3DE8);
  static const Color primaryDark = Color(0xFF4F23C9);
  static const Color primaryLight = Color(0xFFB39BFF);
  static const Color primarySurface = Color(0xFFF1ECFF);

  // Rosas (rol Cuidador / acentos)
  static const Color accent = Color(0xFFEC4C8B);
  static const Color accentDark = Color(0xFFC72670);
  static const Color accentSurface = Color(0xFFFDE8F1);

  // Fondos
  static const Color background = Color(0xFFFAF7FF);
  static const Color cardBackground = Colors.white;

  // Texto
  static const Color textPrimary = Color(0xFF1B1B3A);
  static const Color textSecondary = Color(0xFF5A5A75);
  static const Color textHint = Color(0xFF9A9AB0);

  // Estados
  static const Color success = Color(0xFF22C55E);
  static const Color successSurface = Color(0xFFDCFCE7);
  static const Color warning = Color(0xFFF59E0B);
  static const Color warningSurface = Color(0xFFFEF3C7);
  static const Color danger = Color(0xFFEF4444);
  static const Color dangerSurface = Color(0xFFFEE2E2);

  // Bordes
  static const Color border = Color(0xFFE5E1F2);
  static const Color divider = Color(0xFFEEEAF5);
}
